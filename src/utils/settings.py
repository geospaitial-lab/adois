PADDING_SIZE: int = 320  # padding size in pixels
PADDING_SIZE_METERS: int = 64  # padding size in meters
IMAGE_SIZE: int = 1280  # image size in pixels
IMAGE_SIZE_METERS: int = 256  # image size in meters
RESOLUTION: float = .2  # resolution in meters per pixel
